namespace Test
{
    public class Client : BusinessPartner
    {
        public Client(string name, string address, string phoneNumber, string email) : base(name, address, phoneNumber, email)
        {
        }
    }
}