namespace Test
{
    public class Provider : BusinessPartner
    {
        public Provider(string name, string address, string phoneNumber, string email) : base(name, address, phoneNumber, email)
        {
        }
    }
}